---
description: Connect this Lead Distro AI account (or check the connection) and explain what you can do.
---

Help me connect Lead Distro AI. Use the `lead-distro-ai` skill: check whether the
`mcp__leaddistro__*` tools are loaded. If they are, confirm we're connected and
list what I can do (set up campaigns, onboard buyers/suppliers, ping-post, buyer
portal, automations, reports, audit). If they are not, walk me through connecting
(the plugin auto-registers the MCP; the first tool use opens a browser sign in),
and offer the API-key fallback only if I can't sign in through a browser.
