---
description: Stand up a new Lead Distro AI campaign end to end (type, routing, fields, first buyer and supplier).
argument-hint: [what the campaign is for, e.g. "auto accident leads in CA"]
---

Set up a new Lead Distro AI campaign. Use the `lead-distro-setup-campaign` skill.
If I described the campaign here: $ARGUMENTS — use that as the starting point.
Confirm each write before making it.
